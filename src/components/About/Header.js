import React from 'react';
import './Header.scss';

const Header = () => {
    return (
        <h1 className="about-header">
            <span>Et si</span> 
            <span>on travaillais</span>
            <span>ensemble</span>
        </h1>
    )
};

export default Header;